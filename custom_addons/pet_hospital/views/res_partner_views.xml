<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="res_partner_form_pet_inherit" model="ir.ui.view">
        <field name="name">res.partner.form.pet</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <notebook position="inside">
                <page string="Thú cưng">
                    <field name="pet_ids">
                        <tree>
                            <field name="name"/>
                            <field name="pet_type_id"/>
                            <field name="pet_breed_id"/>
                            <field name="age"/>
                        </tree>
                    </field>
                </page>
            </notebook>
        </field>
    </record>

</odoo>
